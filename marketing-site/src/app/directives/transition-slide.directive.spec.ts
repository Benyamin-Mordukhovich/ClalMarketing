import { TransitionSlideDirective } from './transition-slide.directive';

describe('TransitionSlideDirective', () => {
  it('should create an instance', () => {
    const directive = new TransitionSlideDirective();
    expect(directive).toBeTruthy();
  });
});
